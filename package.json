{
  "name": "h5p-types",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "prettier": "prettier .",
    "format:fix": "npm run prettier -- --write",
    "format:check": "npm run prettier -- --check",
    "lint": "npm run format:fix && turbo run lint",
    "test": "turbo run test"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-config": "github:boyum/prettier-config",
    "turbo": "^1.10.1"
  }
}
