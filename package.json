{
  "name": "h5p-types-root",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "prettier": "prettier .",
    "format:fix": "npm run prettier -- --write",
    "format:check": "npm run prettier -- --check",
    "lint:check": "npm run format:fix && turbo run lint:check",
    "test": "turbo run test",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "typecheck:ts-4": "turbo run typecheck:ts-4"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-config": "github:boyum/prettier-config",
    "turbo": "^1.10.7"
  }
}
