export declare class JoubelMessageDialog {
  /**
   * @param $container The container which message dialog will be appended to
   * @param message The message
   */
  constructor($container: JQuery<HTMLElement>, message: string);
}
